# 🪐 solar-radial-ui

> 방사형(Radial) 구조를 기반으로 한 인터랙티브 UI 컴포넌트 라이브러리
>
> 태양계를 모티브로, 중심(부모) 노드에서 자식 노드들이 궤도를 그리며 배치되고, 클릭 시 계층적으로 탐색할 수 있는 시각적 인터랙션을 제공합니다.

## 프로젝트 개요

`solar-radial-ui`는 **태양계를 모티브로 한 방사형 인터랙션 컴포넌트**입니다.

사용자는 중심 노드(예: 태양)에서부터 자식 노드(예: 행성)를 클릭하며 점점 더 깊은 계층(예: 위성)으로 탐색할 수 있습니다.

각 노드는 클릭 시 중심으로 이동하며 자식 노드들과 함께 새로운 방사형 컴포넌트가 로딩됩니다.

### 시각적 컨셉

- **부모 노드 (중심)**: 태양
- **자식 노드 (1단계)**: 수성, 금성, 지구, 화성, 목성, 토성, 천왕성, 해왕성
- **자식 노드 (2단계)**: 각 행성의 위성 (예: 달, 이오, 타이탄 등)
- **방사형 배치**: 자식 노드들은 중심을 기준으로 원형 궤도에 균등하게 배치
- **인터랙션**: 클릭 시 중심 이동, 페이드 인/아웃 애니메이션

## 🔨 기술적 구현 과정

### 데이터 흐름 구성
```
solarHierarchy (계층 데이터)
    ↓
convertToRadial (RadialNode 변환)
    ↓
calcSolarPositions (좌표 계산)
    ↓
SolarSystemRadial (상태 관리)
    ↓
SolarNode (재귀 렌더링)
    ↓
Circle (시각적 표현)
```
### 데이터 구조 설계 및 위치 계산 로직
**RadialNode** 인터페이스를 통해 계층적 관계를 **level 속성**으로 태양(0), 행성(1), 위성(2)을 구분하였고, 재귀적인 children 구조로 무한한 깊이 표현하였습니다.

**`calcSolarPositions` 함수**에서 선택 상태에 따른 두 가지 모드 구현

- 태양 중심 모드: 태양을 중앙에 배치하고 행성들을 원형으로 배치
- 행성 중심 모드: 선택된 행성을 중앙에 배치하고 해당 위성들을 표시
- 선택되지 않은 노드들은 (-1000, -1000) 좌표로 화면 밖으로 이동시켜 효율적으로 관리

## 🎨 Reference Design

[Pinterest Concept Link](https://pin.it/3bzfug8K7)
